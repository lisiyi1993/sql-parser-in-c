#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
#include <stdbool.h>
#include <time.h>

int main() {
    // srand ( time(NULL) );
    // printf("hello %ld \n", rand());
    // char *tmp = malloc(256);
    // sprintf(tmp, "%d ", 1234);

    // char *str = malloc(1024);;
    // strcat(str, "strings ");
    // strcat(str, "are ");
    // strcat(str, tmp);
    // strcat(str, "concatenated.");
    // printf("hello %s \n", str);

    // char *ptr = "'abcd'";
    // char *str = (char *)malloc(strlen(ptr));

    // strcpy(str, ptr);
    // printf("str = %s \n", str);
    // str = str + 1;
    // str[strlen(str)-1] = '\0';

    // printf("str = %s \n", str);

    // bool b = "123" > "222";
    // printf("%s", b ? "true" : "false");

    printf("%d \n", strcmp("444", "333"));

    return 0;
}